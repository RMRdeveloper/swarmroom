# Changesets

This directory is managed by `@changesets/cli` and records user-visible
package changes for the next Sideroom Pi release.

Create a changeset with `npm run changeset`. Read the full documentation at
https://changesets.dev.
